<UserControl x:Class="WinMaintenanceTool.Views.SettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock Text="{Binding Title}" FontSize="24" FontWeight="SemiBold" />

        <ui:Card Grid.Row="1" Margin="0,14,0,0" Padding="16">
            <StackPanel>
                <TextBlock x:Name="LanguageLabelText" Text="{Binding LanguageLabel}" FontWeight="SemiBold" />
                <ComboBox AutomationProperties.LabeledBy="{Binding ElementName=LanguageLabelText}" ItemsSource="{Binding Languages}" SelectedItem="{Binding SelectedLanguage}" DisplayMemberPath="DisplayName" Margin="0,10,0,8" />
                <Button Content="{Binding ApplyLanguageLabel}" AutomationProperties.Name="{Binding ApplyLanguageLabel}" Command="{Binding ApplyLanguageCommand}" />
            </StackPanel>
        </ui:Card>

        <ui:Card Grid.Row="2" Margin="0,10,0,0" Padding="16">
            <StackPanel>
                <TextBlock x:Name="ThemeLabelText" Text="{Binding ThemeLabel}" FontWeight="SemiBold" />
                <ComboBox AutomationProperties.LabeledBy="{Binding ElementName=ThemeLabelText}" ItemsSource="{Binding Themes}" SelectedItem="{Binding SelectedTheme}" DisplayMemberPath="DisplayName" Margin="0,10,0,8" />
                <Button Content="{Binding ApplyThemeLabel}" AutomationProperties.Name="{Binding ApplyThemeLabel}" Command="{Binding ApplyThemeCommand}" />
            </StackPanel>
        </ui:Card>
    </Grid>
</UserControl>
